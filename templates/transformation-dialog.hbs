<form class="wild-shape-form">

  {{!-- ═══════════════════════════════════════════════════════════════════ --}}
  {{!-- ONBOARDING VIEW                                                    --}}
  {{!-- ═══════════════════════════════════════════════════════════════════ --}}
  {{#if (eq viewState "onboarding")}}

    <div class="wild-shape-section">
      <div class="wild-shape-section-header">
        <label>{{localize "WILDSHAPE.Onboarding.Title"}}</label>
        <span class="wild-shape-count {{#if (gt selectedCount rules.maxForms)}}wild-shape-count-over{{/if}}">
          {{selectedCount}} / {{rules.maxForms}}
        </span>
      </div>
      <p class="wild-shape-hint">
        {{localize "WILDSHAPE.Onboarding.Hint"}}
        (CR ≤ {{rules.maxCRDisplay}}{{#unless rules.allowFly}}, {{localize "WILDSHAPE.Onboarding.NoFly"}}{{/unless}})
      </p>
    </div>

    {{#if noCompendiums}}
      <div class="wild-shape-empty">
        <i class="fas fa-exclamation-triangle"></i>
        {{localize "WILDSHAPE.Error.NoCompendiums"}}
      </div>
    {{else if isLoading}}
      <div class="wild-shape-empty">
        <i class="fas fa-spinner fa-spin"></i> {{localize "WILDSHAPE.Status.Loading"}}
      </div>
    {{else}}
      <div class="wild-shape-beast-list wild-shape-scrollable-list">
        {{#each availableBeasts}}
          <div class="wild-shape-beast-entry {{#if selected}}selected{{/if}}"
               data-action="previewForm"
               data-uuid="{{uuid}}">
            <input type="checkbox" class="wild-shape-beast-check"
                   data-action="{{#if selected}}deselectForm{{else}}selectForm{{/if}}"
                   data-uuid="{{uuid}}"
                   {{#if selected}}checked{{/if}} />
            <img src="{{img}}" class="wild-shape-beast-icon" alt="{{name}}" />
            <div class="wild-shape-beast-info">
              <span class="wild-shape-beast-name">{{name}}</span>
              <span class="wild-shape-beast-cr">CR {{crDisplay}}</span>
            </div>
          </div>
        {{/each}}
      </div>

      <button type="button" class="wild-shape-primary-btn" data-action="confirmForms"
              {{#unless canConfirm}}disabled{{/unless}}>
        <i class="fas fa-check"></i> {{localize "WILDSHAPE.Onboarding.Confirm"}}
      </button>
    {{/if}}

  {{!-- ═══════════════════════════════════════════════════════════════════ --}}
  {{!-- TRANSFORM VIEW                                                     --}}
  {{!-- ═══════════════════════════════════════════════════════════════════ --}}
  {{else if (eq viewState "transform")}}

    <div class="wild-shape-section">
      <div class="wild-shape-section-header">
        <label>{{localize "WILDSHAPE.Transform.Title"}}</label>
        {{#if uses}}
          <span class="wild-shape-uses {{#unless hasUses}}wild-shape-uses-empty{{/unless}}">
            <i class="fas fa-paw"></i> {{uses.current}} / {{uses.max}}
          </span>
        {{/if}}
      </div>
      {{#if noResource}}
        <p class="wild-shape-no-resource">
          <i class="fas fa-exclamation-circle"></i> {{localize "WILDSHAPE.Error.NoResource"}}
        </p>
      {{/if}}
    </div>

    <div class="wild-shape-form-grid">
      {{#each knownForms}}
        <div class="wild-shape-form-card {{#unless ../hasUses}}wild-shape-disabled{{/unless}}"
             {{#if ../hasUses}}data-action="transform"{{/if}}
             data-uuid="{{uuid}}">
          <img src="{{img}}" class="wild-shape-form-img" alt="{{name}}" />
          <div class="wild-shape-form-label">
            <span class="wild-shape-form-name">{{name}}</span>
            <span class="wild-shape-form-cr">CR {{crDisplay}}</span>
          </div>
          <button type="button" class="wild-shape-preview-btn wild-shape-card-preview"
                  data-action="previewForm" data-uuid="{{uuid}}" title="{{localize 'WILDSHAPE.Button.Preview'}}">
            <i class="fas fa-eye"></i>
          </button>
        </div>
      {{/each}}
    </div>

    <button type="button" class="wild-shape-secondary-btn" data-action="editForms">
      <i class="fas fa-pen"></i> {{localize "WILDSHAPE.Transform.EditForms"}}
    </button>

  {{!-- ═══════════════════════════════════════════════════════════════════ --}}
  {{!-- TRANSFORMED VIEW                                                   --}}
  {{!-- ═══════════════════════════════════════════════════════════════════ --}}
  {{else if (eq viewState "transformed")}}

    <div class="wild-shape-section wild-shape-current-form">
      <div class="wild-shape-section-header">
        <label>{{localize "WILDSHAPE.Transformed.CurrentForm"}}</label>
      </div>
      <div class="wild-shape-current-info">
        <img src="{{currentForm.img}}" class="wild-shape-form-img-large" alt="{{currentForm.name}}" />
        <h3 class="wild-shape-current-name">{{currentForm.name}}</h3>
      </div>
    </div>

    <div class="wild-shape-action-buttons">
      <button type="button" class="wild-shape-primary-btn" data-action="revert">
        <i class="fas fa-undo"></i> {{localize "WILDSHAPE.Transformed.Revert"}}
      </button>
    </div>

    {{#if hasUses}}
      <details class="wild-shape-advanced">
        <summary class="wild-shape-details-header">
          <i class="fas fa-exchange-alt"></i> {{localize "WILDSHAPE.Transformed.ChangeForm"}}
        </summary>
        <div class="wild-shape-advanced-content">
          <div class="wild-shape-form-grid">
            {{#each knownForms}}
              <div class="wild-shape-form-card" data-action="transform" data-uuid="{{uuid}}">
                <img src="{{img}}" class="wild-shape-form-img" alt="{{name}}" />
                <div class="wild-shape-form-label">
                  <span class="wild-shape-form-name">{{name}}</span>
                  <span class="wild-shape-form-cr">CR {{crDisplay}}</span>
                </div>
              </div>
            {{/each}}
          </div>
        </div>
      </details>
    {{/if}}

  {{/if}}
</form>
